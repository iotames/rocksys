# 磐石 RockSys 架构底座设计（v2：极简增强式反向代理）

> 本质：底座 = 一台 HTTP 反向代理，等同极简 NGINX `proxy_pass`。
> 唯一核心：老实转发请求。除转发外，**没有任何一样是必须的**。

---

## 1. 设计原则

1. **只有转发是必须的**：接收所有请求 → 转发 → 回传响应。其余一切皆是可选增强。
2. **高可用 = 可紧急关闭**：任何组件掉链子，立即热关闭；关闭后自动降级，转发永不中断。层层摘除，最终退化为裸反向代理，依然在线。
3. **默认零配置可运行**：启动即代理，不需要配置文件、不需要任何组件。
4. **以简驭繁**：能力按需挂载，不用即关，先简单后扩展。
5. **热更即持久化**：任何运行时热更必须「立即生效 + 同步写回配置文件（工作目录 `.env`，开发规范 `bin/.env` / `--config`）」，重启后状态保留。热更不持久化 = 状态丢失，是工程缺陷。

---

## 2. 核心模型：转发链

```
请求 ─▶ [入口] ─▶ [L1 防护] ─▶ [L2 分发] ─▶ [L3 结果] ─▶ 后端
               （可选）        （可选）       （可选）
```

- 三个环节全部是**中间件**，可在线挂载/摘除（hotswap）。
- **全部关闭时**：请求直通默认 upstream —— 行为等同裸 `proxy_pass`。
- **默认 upstream**：启动参数/环境变量指定（如 `127.0.0.1:8080`），未指定用内置默认值。
- **转发必须有超时**（默认 18s，可配置）：慢后端不得挂死代理连接。这是转发引擎基本正确性，非增强。
- **路由语义（等同 NGINX `location /` 兜底）**：
  - 路由未开启：所有请求 → 默认 upstream。
  - 路由开启但未命中：→ 默认 upstream。
  - 路由命中：→ 对应 upstream。

### 2.1 三层时间戳（转发链的测量点，原始需求核心）

三个时间戳是转发链上的固定测量点，仅在观测中间件开启时记录，但**取点语义固定不变**：

```
请求 ─▶ [L1] begin_at ─▶ [L2] begin_biz_at ─▶ [L3] done_biz_at ─▶ 响应
```

- `begin_at`：进入底座的第一时刻，在 L1 任何处理之前。
- `begin_biz_at`：完成路由选择、**即将转发给后端之前**（勿用 defer，防混入上游耗时）。
- `done_biz_at`：收到后端响应、写回客户端之前。
- 耗时分解：防护 = `begin_biz_at − begin_at`；业务 = `done_biz_at − begin_biz_at`；总 = `done_biz_at − begin_at`。
- 精度：同进程内用单调时钟相减，禁止跨进程绝对时间戳相减。

---

## 3. 必需项（仅此两项，不可关）

| 项 | 说明 |
|----|------|
| **反向代理引擎** | 接收全部 HTTP 请求、转发、回传响应；协议级纯转发 |
| **开关机制（hotswap）** | 框架天生能力：中间件/组件在线挂载、摘除、原子切换、排空 |

> 除以上两项外，无任何必须开启的组件。连配置文件都不是必须的。

---

## 4. 可选增强组件（默认全关）

| 组件 | 作用 | 开启时机 | 关闭/摘除后行为 |
|------|------|----------|----------------|
| **L1 防护** | 认证/限流/黑白名单/安全规则 | 需要防护时 | 不过滤，直接转发 |
| **L2 路由分发** | 按 URI 分发到特定后端 | 有多个后端时 | 未命中/未开启 → 默认转发 |
| **L3 结果处理** | 脱敏/统一错误码/响应封装 | 需要统一出口格式时 | 原样回传 |
| **RockScript** | 策略级动态逻辑（Lua） | 需要热改策略时 | 静态规则 / 不执行 |
| **RockConfig** | 配置文件/热重载/集中下发 | 配置项变多、需热更新时 | 仅环境变量 + 启动参数 |
| **RockObs** | 访问日志、统计、耗时分析（原始需求指定组件） | 需要可观测性时 | 不记录、不统计 |
| **RockRegistry** | 动态服务注册与发现 | 后端动态扩缩容时 | 静态路由 / 默认转发 |
| **RockAuth** | 身份认证、租户识别 | 需要鉴权时 | 无鉴权，直接转发 |
| **RockMQ** | 异步消息解耦 | 需要异步链路时 | 同步调用 / Outbox 轮询 |
| **RockObject** | 对象存储 | 需要文件存储时 | 本地磁盘路径 |

> 每个组件三态：**启用 / 精简 / 关闭**。关闭 = 摘除一环，转发链照常。
> 组件开关经 RockConfig（开启时）或环境变量（极简时）下发，热生效、不重启。
> **开关纪律**：关闭 = 降级，不等于瘫痪；开关对业务透明（SDK 内部路由模式）；切换留痕（审计/告警）。
> **配置中心红线**：禁止在项目根目录运行程序（会在根目录残留运行时文件）。运行时文件跟随**工作目录**生成（开发规范工作目录=bin/，实际落点 `bin/.env`、`bin/default.env`，程序源码不写死路径）；`default.env` 为全量默认值快照，装配完成自动同步，`make gen-env` 可主动刷新；参与取值（最低优先级兜底），优先级由 easyconf 决定。

> **两层动态边界（复杂定制业务逻辑用动态脚本）**：
> - Lua（RockScript）：只做**网关策略**（安全规则、路由改写、A/B 分流），不落业务数据、不写业务语义。
> - Python（stbiz_*）：**复杂定制业务逻辑的主战场**。
> - 禁止用 Lua 实现业务逻辑（会把业务塞回底座）。

> **原始需求指定组件**：日志与统计（RockObs）是原始需求明确列出的组件，职责含"分析统计业务应用层、各个微服务，耗时/API/报错统计，日志留存"。极简哲学下它默认关闭、按需开启，但一旦开启，就必须承担"聚合底座 + 各微服务日志"的串联职责（见 §6）。

---

## 5. 最小运行态与降级语义

### 5.1 最小运行态（唯一必需）
```
RockGateway（单进程）──HTTP──▶ 默认 upstream（如 127.0.0.1:8080）
```
启动即用：`rocksys --upstream 127.0.0.1:8080`
无需配置文件、无需数据库、无需任何组件。

### 5.2 降级链（高可用的真正含义）
```
全量能力 ─▶ 关L3 ─▶ 关L2 ─▶ 关L1 ─▶ 裸反向代理（永远可达）
```
任何一环故障 → 热关闭 → 请求绕过该环直通下一级。多级故障也只会退化成"老实转发"，**转发行为永不中断**。

**panic 行为（P3 recover 兜底）**：
- 链中间件（Head/Middle）panic 被 `safeHandle` 兜底：记录中间件名 + 完整堆栈、写 500、中断该请求链，**其余请求零影响**；recover 只兜底不吞错——完整堆栈必入日志。
- ResponseHook（Tail 响应阶段）panic 仅记录日志继续后续 hook（响应阶段可能已写回客户端，**不写 500** 以免污染已发出的响应）。
- `easyserver.ServeHTTP` 外层 recover 为最后防线，兜住转发（Forward）阶段与未来新中间件漏包的 panic，避免击穿到 net/http 导致连接中断。

---

## 6. 串联机制（组件之间的协作通道）

挂件再多也要"串"起来才成系统。串联机制如下：

| 串联机制 | 串起什么 | 极简默认 | 说明 |
|---------|---------|---------|------|
| **请求级数据流 DataFlow** | 请求上下文贯穿三层中间件（trace_id、三时间戳、租户、中间结果） | **内置必需** | 框架能力（复用 easyserver DataFlow），是请求穿过转发链的"车厢" |
| **trace_id 生成** | 入口生成唯一标识，贯穿全链路 | **内置必需**（DataFlow 的一部分） | 优先读 `X-Trace-Id` 请求头，无则生成 32 位 hex |
| **trace_id 透传** | 将 trace_id 注入转发请求头与响应头 | 随 plugins/trace 开启 | 开启后确保上游和客户端都能拿到同一 trace_id |
| **配置/开关下发通道** | RockConfig → RockSwitch → 各组件热更新广播 | 随 RockConfig 开启 | watch + 回调，一处变更串起所有订阅方 |
| **业务内网总线（RockBiz SDK）** | stbiz_* 之间互调（原始需求：微服务间内网通信） | 业务层采用即串联 | 默认 HTTP（一等），gRPC（二等）可选；设计决策：原始需求所述"TCP 协议"在此落实为标准 HTTP/gRPC，以保可观测、可调试、避免私有协议锁定 |
| **日志聚合通道** | 底座日志 + 各微服务日志 → RockObs 统一统计 | 随 RockObs 开启 | 满足原始需求"分析统计各个微服务" |

> 唯一必需的串联是 **DataFlow**（转发链的载体，属框架本身）；其余随对应组件开启而存在，全部可关。

---

## 7. 数据流动

### 7.1 主链（唯一必然存在的流）
```
Client ──HTTP──▶ 入口 ──转发──▶ 默认/路由后端 ──▶ 响应回传 ──▶ Client
```

### 7.2 增强链（组件开启后才存在）
```
路由链：请求 ─▶ 路由表匹配 ─▶ 命中指定后端 / 未命中默认后端
防护链：请求 ─▶ 认证/限流 ─▶ 通过则转发，拦截则直接响应
观测链：请求 ─▶ 各中间件打点 ─▶ RockObs 记录/聚合（开启时）
配置链：RockConfig 变更 ─▶ watch ─▶ 热更新组件与路由
```

### 7.3 业务层内部调用链（原始需求：微服务间内网通信）
```
stbiz_A ──(经 RockBiz SDK)──▶ HTTP 内网调用（默认）/ gRPC（可选）──▶ stbiz_B
          请求带 trace_id、超时、幂等键；禁止裸私有 TCP 协议
```

### 7.4 日志聚合链（原始需求：分析统计各个微服务）
```
底座访问日志 + 各 stbiz_* 结构化日志 ──▶ RockObs 聚合
      → 耗时/API/报错统计 → 查询 API（默认）
      → 导出 Prometheus / Loki / Grafana（升级，可选）
```

---

## 8. 组件关系与红线

### 8.1 依赖方向
```
客户端 ─▶ RockGateway（转发链）──▶ 后端服务
             │ 可选挂载
             ▼
   RockConfig / RockObs / RockRegistry / RockAuth / RockMQ / RockObject
```
- 底座不依赖任何组件；组件是底座的"挂件"，摘除不影响底座。
- 组件之间禁止反向调用底座核心。

### 8.2 红线
| 禁止项 | 原因 |
|--------|------|
| 将可选能力写进转发核心 | 破坏极简与可摘除性 |
| 底座依赖业务运行时 / 业务数据库 | 转发层必须纯粹 |
| 自研私有传输协议 | 用标准 HTTP，保持可观测、可调试 |
| 绑定任何厂商/云服务 | 一切组件自包含，第三方仅走适配器 |

> **设计决策：原始需求所述"TCP 协议"落实为标准 HTTP/gRPC**。原因：(1) 标准协议自带可观测性（抓包/日志/链路追踪）；(2) 避免私有协议锁定，降低 stbiz_* 开发门槛（任何 HTTP 客户端即可调用）；(3) gRPC 作为二等公民提供高性能选项。原始需求中"微服务间内网通信"的意图保留，仅协议标准化。

### 8.3 底座边界（坚决不做，极简的守护）
| 底座不做 | 原因 |
|---------|------|
| 承载业务逻辑 | 业务在 stbiz_* |
| 直连业务数据库 | 数据读写归微服务 |
| WebSocket | Upgrade 握手（101）后进入双向字节隧道（Engine.Forward 劫持连接对拷），ws 帧原样透传；其余长连接由旁路服务承担 |
| 大文件上传/下载中转 | 避免二进制流占用代理 |
| 定时任务 / 批处理 | 独立 worker 承担 |
| 自研传输层 / 数据库 / 缓存 | 只做协议级转发，不造轮子 |

> **数据访问层（internal/db + easydb）**：底座本身仍不直连业务数据库（红线不破）。
> 该层服务的是**可插拔的串行组件**（如 mq 的 outbox 存储）——组件需要持久化时经统一数据访问层
> 操作，SQL 语句外置到 `sql/<dbtype>/` 目录（外挂统一根 `HOT_SCRIPTS_DIR` 下 `sql/` 子目录优先、编译期嵌入兜底），切换数据库缺脚本即报错。
>
> **数据库铁律**（详见 `docs/DEV_HANDBOOK.md`）：① SQL 必须落盘 `sql/<dbtype>/`，禁止 Go 内联；② 换库只改 `bin/.env`（`DB_DRIVER`/`DB_DSN`）；③ 纯 SQL 参数化，不用 ORM/对象模型；④ sqlite/mysql/postgres 三方言齐平，缺脚本即报错。
>
> **数据字典**：全部业务表（`shield_event`/`access_log`/`admin_users`/`outbox`/`ip_blacklist`/`ip_whitelist`/`attack_archive`，共 7 张）字段与枚举（含三方言类型对照）见 `docs/DATA_DICT.md`；数据结构变动（含新增枚举）必须同步维护，红线见 `AGENTS.md`。

### 8.4 演进原则
**演进 = 开关切换，不是重构**：最小 → 标准 → 完整是同一套代码的开关组合；任何阶段关闭某组件即回到上一级能力，转发永不中断。

---

## 9. 第三方扩展（SPI / 适配器）

| 扩展点 | 抽象接口 | 默认（自包含） | 第三方对接 |
|--------|----------|---------------|-----------|
| 配置 | KV + Watch | 环境变量/本地文件 | etcd / Consul / Apollo |
| 注册 | KV + 健康检查 | 静态表 | etcd / Consul / Nacos |
| 认证 | OIDC / SAML | 内置 JWT | 集团 IDaaS / 企业微信 |
| 消息 | Producer / Consumer | Outbox 轮询 | Kafka / RocketMQ |
| 观测 | 日志/指标导出 | 本地文件 + 内存聚合 | Prometheus / Loki / Grafana |
| 对象存储 | S3 兼容 API | 本地磁盘 | MinIO / 任意 S3 |

原则：底座只依赖接口与标准协议；第三方故障自动回退默认实现。

---

## 10. 工程化（简短）

- **底座**：Go，转发引擎 + 热插拔中间件链（复用 easyserver head/middle/tail + DataFlow 模式）。
- **业务层**：stbiz_* 微服务（Python/PHP），DDD，独立数据库；**不用 SDK 也能被代理**（此时仅裸转发）。
- **业务层内网通信**：stbiz_* 互调经 RockBiz SDK，默认 HTTP（一等公民），gRPC（二等公民）可选；调用必须带 trace_id、超时、幂等键；禁止裸私有 TCP 协议。
- **契约**：HTTP 用 OpenAPI；gRPC 为二等公民，按需启用 Proto。
- **验证**：单元测试 + 契约测试 + 全链路压测（重点验证"关闭某组件后转发不中断"）。

---

## 11. 一句话总结

**底座 = 永不宕机的反向代理**：默认老实转发到默认端口，路由是可选、防护是可选、观测是可选——全部可热关；任何环节掉链子，紧急关闭，转发依旧。这才是真正的高可用。
