# 可信代理列表：每行一个 IP 或 CIDR 网段（# 注释、空行忽略）。
# 直连源 IP（TCP 层）命中此列表时才信任 X-Real-IP / X-Forwarded-For 转发头，
# 防止公网直连时伪造转发头；未命中时直接返回直连源 IP。
#
# 外挂覆盖：工作目录 trusted_proxies/ 下同名文件优先（TRUSTED_PROXIES_FILE 配置
# 相对路径），缺失/为空时回退本内嵌文件。
#
# 默认可信代理：本机 Nginx 转发场景（rocksys 与 Nginx 同机，经 127.0.0.1 转发）。
# 若 Nginx 在其他机器/容器，请将代理地址或网段（如 10.0.0.0/8）加入外挂文件。
127.0.0.1
