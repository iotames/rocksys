# RockSys 管理控制台 产品设计方案

> 面向**平台运维与管理员**的 Web 控制台。本文档描述控制台的功能、页面与交互设计，不含任何技术实现细节。

---

## 1. 产品概述

### 1.1 产品定位

RockSys 磐石是"**永不宕机的反向代理**"，其能力由多个可热开关的组件（挂件）组成。本控制台是 RockSys 的**图形化管理入口**，让管理员在浏览器中完成日常全部运维操作：

- 全部**数据流组件**（9 个：防护 / 透传 / 认证 / 分发 / 改写 / 脚本 / 观测 / 抄送 / 结果）与**独立服务**（4 个：配置服务 / 注册 / 存储 / 消息，消息组件按配置条件装配）的**开启 / 关闭**与状态监控
- 网关与全局基础设施的**配置修改**（即时生效，无需重启）；各组件/服务配置在各自页面
- 策略脚本的**发布与回滚**
- 网关**运行指标**（概览页）与**入网数据 / 系统日志 / WAF安全**的查看

核心产品理念贯穿所有页面：**"关闭只是降级，转发永不中断"**。

### 1.2 目标用户

| 角色 | 使用场景 | 关键诉求 |
|------|----------|----------|
| 平台运维 / 管理员 | 日常巡检、故障定位、安全加固 | 一眼看清系统是否正常；快速启停组件；查日志、看指标 |
| 平台研发 | 配置路由 / 防护 / 改写，发布策略脚本 | 可视化配置、脚本版本管理 |
| 交付实施人员 | 现场部署调优 | 免命令行，图形化交付，降低上手成本 |

### 1.3 设计目标

1. **全覆盖**：RockSys 的全部管理能力在控制台中均有入口（见 §6 功能覆盖清单）。
2. **一目了然**：打开首页即知系统是否健康，5 秒内完成巡检。
3. **操作有护栏**：一切影响全局的操作必须二次确认，成功或失败都有明确反馈。
4. **中文界面**：全部界面为简体中文，术语与产品文档一致。

### 1.4 非目标

- 不面向业务终端用户（业务系统有各自界面）。
- 不做多台网关的统一集群管理（本控制台管理单台网关）。
- 不承担业务数据管理。

### 1.5 实现技术形态

| 项 | 说明 |
|----|------|
| 界面风格 | **ElementUI 风格**（中后台经典样式，观感与 ElementUI 组件一致） |
| 实现方式 | 纯静态单页：HTML + CSS + 原生 JavaScript，**不引入 Vue 等前端框架**、无构建链 |
| 唯一 vendor 依赖 | **Apache ECharts**（地理位置热力地图用）：`webui/assets/vendor/echarts.min.js` v5.6.0（Apache-2.0），下载直链 `https://registry.npmmirror.com/echarts/5.6.0/files/dist/echarts.min.js`（npmmirror 为 npm 官方包的国内镜像），升级时手工替换文件。地图数据 `webui/assets/geo/{world,china}.json` 取自 echarts v4.9.0 包内 `map/json/`（ECharts 5 起不再随包发行地图），直链 `https://registry.npmmirror.com/echarts/4.9.0/files/map/json/world.json` 与 `https://registry.npmmirror.com/echarts/4.9.0/files/map/json/china.json`；其中 world.json 已做预处理——区划名 `name` 由英文改为 ISO 3166-1 alpha-2 码（原名保留在 `name_en`），与后端 geo 聚合输出的 country_code（ISO2）直连 |
| 交互还原 | 内置 ElementUI 主题样式（静态资源），按钮 / 表格 / 弹窗 / 开关等交互由原生 JS 实现，视觉与交互贴近 ElementUI |
| 交付形态 | 双模式：**生产**全部静态资源内嵌进 rocksys 单二进制（`go:embed`），随网关一起分发，不额外部署、不依赖外网；**开发**（`-tags dev` 编译）实时读 `webui/` 源码目录，改前端文件后刷新浏览器即见、免重新编译 |
| 界面入口 | 网关管理地址（如 `127.0.0.1:19527`），浏览器直接打开控制台 |
| 请求方式 | 控制台只调用网关自身的管理接口（同源），不访问任何第三方服务 |

> 产品设计角度：无论底层如何实现，用户看到的都是"打开一个网址即得控制台"。技术形态只影响交付与部署，不影响本设计中的任何页面与交互。

---

## 2. 设计原则

| 原则 | 含义 | 落地 |
|------|------|------|
| **转发第一** | 网关是否在线是最高优先级信息 | 首页顶部常驻网关状态与数据流展示 |
| **开关即能力** | 组件以"开关"为第一视角，配置为第二视角 | 组件页 = 状态/配置双页签，首页卡片开关直启 |
| **改配即生效** | 配置保存后即时生效，不重启 | 保存后明确提示"已即时生效" |
| **失败可解释** | 操作失败必须说明原因 | 失败提示展示具体错误原因 |
| **提示分级** | 正常反馈轻打扰、异常信息重提示 | 提示统一走 `ui.toast` 唯一组件（见 pages.md §4.10）：成功/信息自动消失，错误/警告常驻需手动关闭 |
| **贴近用户** | 界面只讲功能，不讲技术 | 页面用中文功能语言，不出现技术术语 |
| **ElementUI 风格** | 界面统一采用 ElementUI 视觉风格，中后台观感一致 | 全部界面遵循 ElementUI 风格规范（样式与组件观感），不引入前端框架 |

---

## 3. 信息架构与导航

```
RockSys 控制台
├── 概览            ← 默认落地页（页签「总览」：网关信息横条 · 流量统计（含地理位置）· 运行状态（实时指标+趋势+资源）· HTTP 数据流图 · 组件/服务状态总览；页签「小黑屋」：在押封禁预览）
├── WAF安全     ← 安全巡检：攻击拦截 / 黑白名单管理 / 文件编辑
├── 入网数据        ← 流量巡检：HTTP 入网请求日志
├── 组件
│   ├── 防护 shield / 透传 trace / 认证 auth / 分发 dispatch
│   ├── 改写 rewrite / 脚本 script / 观测 obs / 抄送 copy / 结果 result
├── 服务
│   └── 配置服务 config / 注册 registry / 存储 object / 消息 mq
├── 脚本             策略脚本发布 / 回滚
└── 配置
    ├── 全局配置      网关 / 数据访问 / 其他；组件与服务的配置入口链接
    ├── 数据库 database  ← 表结构检查 / SQL 执行（表结构同步）
    ├── 定时任务 schedule ← 只读登记与状态汇总（GEOIP_LIST 方案）
    └── 系统日志      ← 排障入口：系统进程实时日志
```

导航采用**左侧固定侧边栏**，顶部为全局工具条。顶栏左侧 ☰ 按钮可**收起/展开侧边栏**（收起后主内容占满全宽，偏好记忆于 localStorage，刷新不丢）。「组件 / 服务 / 配置」为可折叠分组；组件与服务每个条目对应一个独立页面（中文 + 英文命名），分组内激活项所在分组自动展开。WAF安全 / 入网数据为顶级菜单（原「观测」分组已提升，URL 不变）；全局配置 / 数据库 / 系统日志收拢进「配置」分组（URL 均不变）。

各页面定位：

| 页面 | 用户任务 | 信息量 | 使用频率 |
|------|----------|--------|----------|
| 概览 | 巡检：系统是否正常，链路各环节状态 | 高 | 每次打开 |
| 组件 | 启停与配置数据流各环节能力 | 中 | 中 |
| 服务 | 启停与配置独立支撑服务 | 中 | 中 |
| 脚本 | 发布 / 回滚策略 | 中 | 低 |
| WAF安全 | 安全告警与拦截追溯 | 中 | 中 |
| 入网数据 / 系统日志 | 排查单个请求 / 进程问题 | 高 | 中 |
| 全局配置 | 调整全局基础设施参数 | 高 | 低 |

---

> 本文档拆分目录索引。找页面/交互细节先查下表定位子文件，再在子文件内按章节号检索：

| 文件 | 内容 | 何时读 |
|------|------|--------|
| [pages.md](pages.md) §4.1-4.2 | 整体框架（侧边栏/顶栏/登录）、概览页（网关信息/运行状态/流量统计/数据流/小黑屋） | 全局布局与概览页行为、流量统计加载/降级细节 |
| [pages.md](pages.md) §4.3-4.9 | 组件·服务详情页、全局配置（含搜索）、脚本、入网数据、数据列表公共交互、可信代理、系统日志 | 各页面结构与交互闭环 |
| [pages.md](pages.md) §4.10 | 提示消息规范（toast 唯一组件、error 常驻、豁免清单、加载指示条） | **任何前端提示/报错处理必读**（AGENTS.md 体验红线引用处） |
| [pages.md](pages.md) §4.11 | 数据库页（表同步/表概览/表数据/SQL历史四页签） | 数据库相关页面设计与后台任务交互 |
| [pages.md](pages.md) §4.12-4.15 | WAF 黑白名单与封禁、UA 名单行级管理、开关设置原则、跳转链接可点击性规范 | WAF 页改动、开关取舍、链接样式 |
| [pages.md](pages.md) §4.16-4.17 | 定时任务（schedule 只读页）、后台任务（任务中心观测页） | 任务登记/观测页 |
| [interaction.md](interaction.md) §5 | 交互与界面规范（状态色/开关/表格/加载/空态/数字格式/配置输入控件/前端校验） | 通用界面规范 |
| [interaction.md](interaction.md) §7 | 视觉设计规范（主题 data-theme、深色色值、排版间距、卡片） | 视觉/主题改动 |

## 6. 功能覆盖清单

以下为 RockSys 全部管理能力在控制台中的覆盖情况（各组件配置详见对应配置分组）：

| 管理能力 | 覆盖位置 |
|----------|----------|
| 查看网关运行信息（端口、后端、超时） | 概览·网关信息 / 全局配置·网关 |
| 修改网关配置并即时生效 | 全局配置·网关 |
| 全部数据流组件（9 个）与服务（4 个，消息按配置装配）的启停与状态 | 组件/服务详情页 / 概览·HTTP 数据流（组件）与服务总览 |
| 各组件/服务各自的配置项查看与修改 | 组件/服务详情页·配置页签 |
| HTTP 数据流顺序可视化（组件穿插链路） | 概览·HTTP 数据流 |
| 策略脚本发布（含网关重启后需重新发布的提示） | 脚本页 |
| 策略脚本回滚与版本管理 | 脚本页·版本时间线 |
| 实时运行指标（请求量、延迟分位、错误率）与趋势 | 概览·运行指标（原独立"指标"页已合并） |
| 流量统计（请求次数/PV/UV/独立IP/拦截/攻击IP/4xx/5xx 及率，访问与拦截趋势图、Top 国家地理分布；时间范围预设 + 自定义；GeoIP 缺失引导） | 概览·流量统计区（`/admin/obs/traffic/summary` `/series` `/geo` 三端点，见 `docs/api/obs.md`） |
| 入网数据日志按时间范围（精确到分）+ 路径查询 | 入网数据 |
| 日志按状态码/耗时排序/只看异常筛选 | 入网数据 |
| 单条日志详情（分环节耗时、转发目标、复制） | 入网数据·详情弹层 |
| 系统进程实时日志 | 系统日志 |
| WAF 拦截监控（实时计数窗口桶宽 1m/5m/15m/1h 可切、按日趋势、Top 攻击源、拦截明细追溯、本次运行落库/落库总数双口径） | WAF安全 |
| Top 攻击源 IP（Top N 可选 10/20/30/50/100，改即拉）+ 批量加黑（勾选列 + 一键加入黑名单，已在黑名单的行标注且不可选；一次 import 批量导入、幂等跳过重复）+ 地区列（country_name/city，GeoIP 未加载显示「—」） | WAF安全·攻击拦截（`/admin/shield/stats` 的 `top` 参数 1-100；返回 `in_blacklist`/`blacklist_addable`；加黑走 `POST /admin/shield/blacklist/import`，`block_type=11` 人工收录、永久生效、快照重建即时拦截） |
| 动态 IP 黑白名单管理（列表/新增/软删恢复/批量导入/从文件同步/排序/分页过滤） | WAF安全·黑白名单 Tab（`/admin/shield/blacklist` 与 `/whitelist`，含 `sync_file` 从文件同步与 `sort` 排序；DB 未配置时页内提示不可用；交互细节见 pages.md §4.12） |
| UA黑名单/UA白名单行级管理（规则文件：生效模式表格/单条删除/追加/恢复默认，≤3s 热更） | WAF安全·黑白名单 Tab·UA 子页签（`views/ualist.js`，读写 `/admin/shield/rules*` 三端点；UA白名单无开关有数据即生效、仅豁免爬虫 UA 拦截一步；见 pages.md §4.13） |
| 拦截明细行内封禁（操作列「IP封禁」+ 行详情弹层入口，24h/永久单选，封禁次数累计满 5 转永久） | WAF安全·攻击拦截（`POST /admin/shield/blacklist/ban` 专用封禁端点；弹窗与置灰逻辑见 pages.md §4.12） |
| 首页小黑屋预览（当前在押全部封禁条目，临近解封在前、永久殿后） | 概览·小黑屋页签（`GET /admin/shield/jail`，见 pages.md §4.2） |
| 自动拉黑（按风险分档：攻击类命中 1 次直接永久；爬虫 UA/通用类窗口内达各自阈值自动入黑名单，累犯转永久；随 `SHIELD_ENABLED` 热联动，防护关闭即不生效） | 配置 `SHIELD_AUTO_BAN_*` 六项（`docs/CONFIGURATION.md`）；命中结果在概览·小黑屋与黑白名单列表可见 |
| WAF 规则文件在线编辑（risk_paths.txt / crawler_ua.txt / ua_whitelist.txt 等 7 个规则文件，含外挂/内置生效状态与生效行数） | WAF安全·文件编辑 Tab（`/admin/shield/rules*` 三端点；保存落点 `HOT_SCRIPTS_DIR/rules/`，ScriptHub ≤3s 自动热更生效，无需重启；编辑器复用 codeEditor 公共组件；UA 两个名单文件另在黑白名单 Tab 有行级管理入口，见 pages.md §4.13） |
| 可信代理列表在线编辑（IP/CIDR 白名单，保存前服务端解析校验非法内容拒绝） | 全局配置·可信代理页签（`/admin/proxy/trusted*` 三端点；保存落点 `HOT_SCRIPTS_DIR/trusted_proxies/`，ScriptHub ≤3s 自动热更生效；页面骨架与规则文件编辑共用 fileEditor 公共视图工厂） |
| 数据库表结构同步（检查 A-F 分级差异、编辑生成 SQL、danger 强确认执行、复核闭环；存量升级不再手工 ALTER） | 服务→数据库·表同步页签（`/admin/db/schema` 与 `/admin/db/exec` 两端点；执行为 danger 级操作，服务端不做语句白名单） |
| 账号登录 | 登录视图（账号密码）；非回环地址下进入控制台前需登录，JWT 失效后自动跳转登录视图 |

> 以上管理能力在控制台全部可用，无遗漏。

---

## 8. 关键操作流程

### 8.1 开启一个组件

```
组件页 → 点击"开启" → 确认框"开启后将参与请求链路，规则立即生效"
→ 确认 → 提示"已启用" → 列表刷新（状态变绿）
失败 → 提示"开启失败：<具体原因>" → 状态不变
```

### 8.2 修改配置

```
配置页 → 选择组件分组 → 点击"修改" → 输入新值 → 保存
→ 提示"⚡ 已即时生效，无需重启" → 该项标记已更新
```

### 8.3 发布脚本并回滚

```
脚本页 → 编写内容 → 点击"发布" → 版本 +1，提示"已发布 v3"
→ 需要恢复 → 版本时间线选择旧版本 → 确认 → 生效版本回退
```

### 8.4 日常巡检

```
打开控制台 → 概览：网关是否在线？数据流组件是否异常关闭？请求量与错误率是否异常？
→ 异常追查：组件页调整启停 → 入网数据 / 系统日志页看指标与日志
```

## 9. 使用体验要求

| 项 | 要求 |
|----|------|
| 登录 | 管理接口绑定非回环地址时需先登录（账号密码）；凭证失效后跳转登录视图重新登录；回环地址本机免登录 |
| 刷新 | 各页面自带页内刷新按钮（概览 / WAF安全 / 全局配置 / 脚本 / 日志 / 文件编辑 / 组件·服务详情等）；顶栏不设全局刷新控件 |
| 网关不可达 | 仅网络层失败（连接失败/DNS 解析失败等 fetch 层错误）触发顶部常驻提示"管理接口不可达"，页面保留上次数据并标注时间；**请求超时不算不可达**（服务端已响应、只是慢查询未返回，避免误报服务故障），超时文案走 toast；服务端有响应（含 5xx）同样不算不可达，错误文案走 toast 展示（后端统一返回 `{"ok":false,"error":...}` JSON） |
| 静态资源缓存 | 静态资源统一 `Cache-Control: no-cache`（协商缓存）：dev 模式「改前端刷新即见」依赖此头，否则浏览器启发式缓存旧 JS/CSS 会导致改了没生效；生产升级后也避免执行旧前端代码 |
| 浏览器 | 支持主流浏览器（Chrome / Edge / Firefox 较新版本） |
| 打开速度 | 首次打开即可使用，无外部资源依赖 |
| 数据安全 | 凭证仅保存在本机浏览器；敏感配置默认掩码 |

---

## 10. 一句话总结

**这是一个看得见的"总控开关面板"**：网关状态、HTTP 数据流常驻首页，组件与服务卡片开关直接启停，配置各归其位，脚本一眼可回滚，指标趋势、安全防护、入网数据与系统日志随手可查——所有操作都在传达同一句话：**关闭只是降级，转发永不中断。**
