# A7 · 前端：首页 tabs + 小黑屋页签

> 实施状态：**待实施**
> 前置：A3 已实施（jail 端点）。设计依据：决策 13、§3.7。

## 2. 改动文件清单

| 文件 | 动作 | 说明 |
|---|---|---|
| webui/assets/js/views/overview.js | 修改 | 引入 tabs + 小黑屋子视图 |

已有文件：刷新即见。

## 3. 实施步骤

- [ ] overview.js 引入 `Rock.comp.tabs`：现有内容（metrics/组件卡片/图表/自动刷新）整体归入默认页签「**总览**」，行为不变
- [ ] 新页签「**小黑屋**」：
  - [ ] 说明行：当前在押的限时封禁（未过期、未软删）；`created_at` 为首次封禁时间
  - [ ] `GET /admin/shield/jail?limit=20` 拉取；dataTable **client 模式**（不分页）；列：封禁 IP / 封禁原因（typeName）/ 命中次数 / 封禁次数 / 封禁时间（首次）/ 解封时间
  - [ ] 空态：empty 组件「小黑屋空空如也」
  - [ ] 计数行「共 N 条在押」（jail total，超出 20 提示还有更多）+ 「管理全部黑名单 →」链接跳黑白名单页
  - [ ] 拉取时机：切到页签时拉取 + 随首页自动刷新周期联动刷新（**自动刷新由 main.js `restartAutoRefresh` → `refreshPage(r, {silent})` 全局驱动，overview.js 自身无定时器**——联动即把小黑屋刷新挂进 overview 的 load/刷新路径）；「总览」页签刷新行为不受影响
- [ ] 小黑屋子视图建议抽独立函数/模块（overview.js 内或 views/jail.js 子视图），避免 overview 主函数膨胀

## 4. 验证（浏览器回归）

- [ ] 首页两页签切换正常；「总览」metrics/图表/自动刷新与改造前一致
- [ ] 小黑屋·有在押：六列数据正确（用 A6/A4 造的封禁数据核对：原因类别、次数、时间）
- [ ] 小黑屋·空态：文案 + 空态组件
- [ ] 「管理全部黑名单 →」跳转正确；计数与实际一致
- [ ] 页签停留小黑屋时随自动刷新更新；路由切换再回来数据不残留旧态

## 5. 完成标准

清单全勾 + 验证全过 → 状态「已实施」→ 更新总纲。

## 6. 实施回填区（中断现场记录）

（空）
