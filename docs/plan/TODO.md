# 执行总纲

> 执行依据：docs/plan/README.md（计划目录工作宪法）——记号语义、状态机、关口与裁决以宪法为准。

项目状态：进行中

## §0 断点续传（接手者从这里开始）
1. 读项目状态行与 §1 总表：
   - 本项目不拆 STEP 子文档（母文档头部显式豁免宪法 §1 拆步建纲），**执行单元 = EGRESS_TIMING_PLAN.md §4 四阶段**，总表各行指向母文档对应阶段节；「已实施」判定与验证命令以母文档各阶段「验证」条目为准；
   - 某项目全部阶段「已实施」→ 终验（母文档 §4「终验」+ §5 验收标准）与呈报未做则先做，改项目状态「待人类验收」，此后停止一切实施动作；
   - 状态为「待人类验收/等待人类」→ 不实施任何动作，向用户呈报等待点；
2. 定位第一个非「已实施」的阶段；含受阻标注时读母文档 §5 变更记录与代码现场，未解除则转向总表中下一个不依赖它的阶段；
3. 若为「实施中」，先跑母文档该阶段「验证」条目预写的命令（无副作用、可重复执行）核实已完成部分完好，从首个未完成项继续，禁止重做；
4. 若为「待实施」但代码现场已有该阶段产物命中（中断发生在动作落地后、记录保存前）→ 先核实产物再续做，禁止从零重写覆盖现场；
5. 每次接手核实后、每次状态迁移收口时，核对总表状态列与母文档 §5 变更记录一致（收口三同步：总表状态、变更记录一行、阶段产物勾选事实）；
6. 严禁凭会话记忆做任何实施判断。

## §1 状态总表（执行单元 = EGRESS_TIMING_PLAN.md §4 阶段，无 STEP 子文档）
| # | 阶段 | 内容 | 依赖 | 状态 |
|---|------|------|------|------|
| 1 | 阶段一（母文档 §4） | DataFlow 第 4 埋点：done_at key、SetDoneAt/DoneAt/EgressMs、TotalMs 口径修正 + 单测 | — | 已实施 |
| 2 | 阶段二（母文档 §4） | chain 取点与写回后回调：DoneHook 接口、adapter SetDoneAt + 步骤 9b OnDone | 1 | 已实施 |
| 3 | 阶段三（母文档 §4） | obs 记录迁移 + 数据层 egress_ms 列：obs.go/dim.go/db_store.go/admin.go + 三方言 SQL | 1,2 | 已实施 |
| 4 | 阶段四（母文档 §4） | 前端展示 + 文档红线收口：logs.js 四段拆解与排序 + DATA_DICT/HTTP_DATAFLOW/webui.md | 3 | 已实施 |
| 5 | 终验（母文档 §4/§5） | 全量测试 + vet + 生产构建 + dev 实机验收 + 文档字段核对 | 1,2,3,4 | 已实施 |

依赖列＝前置阶段编号；受阻时「转向不依赖它的阶段」以本列为准。

## §2 执行期红线（自仓库法摘录，冲突时以仓库法原文为准）
- 构建/测试/静态检查一律原生命令行（`go build` / `go test` / `go vet`），禁用 make。
- 运行目录红线：程序必须在 `bin/` 目录运行，严禁项目根目录执行。
- 改 `sql/` 后必须同步 `cp -r sql/* bin/hotscripts/sql/`（外挂优先红线）。
- 数据字典红线：数据层变动三处同步——三方言脚本、docs/DATA_DICT.md、Go 权威定义。
- 文档同步清单：DATA_DICT.md、HTTP_DATAFLOW.md、webui.md、代码注释（含 easyconf 注册 title/usage）。
- git 提交：本次任务**不适用**破例授权（用户未授予，提交前照常请示），见宪法 §3.9。

## §3 进度日志（一行一事）
| 日期 | 执行者 | 阶段 | 结果 |
|------|--------|------|------|
| 2026-09-05 | ZCode | — | 设计定稿（人类确认通过），创建本总纲，尚未开工实施 |
| 2026-09-05 | ZCode | 1-5 | 四阶段+终验全部完成：测试/vet/生产构建全过；实机验证四段拆解与出网排序生效（发现并补齐 admin.go sort 白名单遗漏，见母文档变更记录）；已提交 Git（用户授权） |
