# 验收与修复过程记录（2026-08-30，本文件不入库）

对应提交：`bc6c9e1`（已推送 master）。范围：最近两次提交（e9e3ee9 表结构同步、4a53bdd IP 黑名单增强）的代码审核问题修复 + 按 `IP_BLACKLIST_PLAN.md` / `DB_SCHEMA_SYNC_PLAN.md` 两份母文档做功能性验收。两份母文档按要求保留；`docs/plan/`（TODO 总纲 + 13 份 STEP 文档）已验收通过后删除。

## 一、代码审核问题的修复

### 表结构同步（e9e3ee9 引入）
1. **pg catalog 自增识别（高）**：`sql/postgres/schema_query_columns.sql` 的 `d.adbin LIKE '%nextval%'` 不可运行。审核建议改 `::text`，但真库验收发现 `adbin` 是 `pg_node_tree`，`::text` 得到的是解析树序列化（不含 nextval 字样），最终改为 `pg_get_expr(d.adbin, d.adrelid) LIKE '%nextval%'`。此问题若上线，pg 部署首次跑「表结构检查」即 500。
2. **表级约束缺列误判 B 级（中）**：mysql 表级 `UNIQUE KEY uk(col)`、`PRIMARY KEY(a,b)` 不产出 ColumnDef，缺列会被 B 级自动补列、唯一约束静默丢失。新增 `db.ParseTableKeyColumns` 提取表级键约束列，命中判 C 级需人工。`ColumnDef` 新增 `IsUnique`（精确关键字匹配，替代原 `Contains(Raw,"UNIQUE")` 误报源）。
3. **D 级整份索引脚本重放（中）**：与执行器「遇错即停」矛盾——重放会在第一个已存在索引处报 Duplicate key，缺失的索引反而永远执行不到。改为仅生成缺失索引的单条 `CREATE INDEX`（`GenerateSQL` D 分支重写，note 文案同步更新）。
4. **A 级不建索引（验收新发现）**：A 级只生成建表原文，全新库一轮同步后仍缺全部索引（真库实测剩 12 个 D 级），需二次检查才能补齐。改为 A 级附带配套索引脚本（与组件 `EnsureTable`「建表+索引」同语义），一轮闭环。
5. **exec 端点加固（中）**：`/admin/db/exec` 加进程内互斥（在途回 409）、`http.MaxBytesReader` 1MB 上限、`ExecContext(r.Context())`（停机可取消）。
6. 低优先：`tokenize` 先归一 Tab/换行（防 `NOT\tNULL`、`DECIMAL(10,\n2)` 误切）；`SHIELD_EVENT_TABLE` 配置值加 `^[A-Za-z_][A-Za-z0-9_]*$` 校验兜底（非法回落默认表名）；`schema_diff.go` 注释引号笔误。

### IP 黑名单（4a53bdd 引入）
7. **热更开启永不生效（中）**：`autoBan.Start()` 仅在装配时 `Enabled()` 才调用，false 启动后运行期改 true 永不启动 goroutine，与「热更生效」承诺矛盾。已改无条件启动。
8. **配置字段数据竞争（中）**：conf 热更直写注册指针，引擎 goroutine 无同步读取。改为每轮经 `conf.Manager.List()`（内部持锁）读快照，注册指针仅作测试注入口（fake 未实现 List 时回落直读）。`-race` 全量通过。
9. **引擎启动时点（race 修复引出的连带决定）**：Start 从装配中间移到 `buildServer` 末尾（全部注册完成后）——装配期 easyconf 注册路径非并发安全，goroutine 过早启动会与后续 `Register` 竞争（`-race` 实测抓出）。
10. **warn_times 原子自增（低）**：`RestoreBan` 原「读-改-写」三方言脚本改为 `warn_times = warn_times + 1`（少一个参数），多写方（自动拉黑 + 人工）并发安全；转永久判定仍基于读取时快照，极端并发下最多延后一轮追加标记（注释已说明）。
11. **title 列宽（低）**：新增 `truncateTitle`（64 字符，utf-8 计），insert/update/import/restore_ban 全部入库路径收口；转永久后缀追加前预留空间。防 MySQL 严格模式超长报错导致续封每轮失败。
12. **block_type 越界 500→400（E2E 验收发现）**：新增接口显式传 `block_type=12` 原落 mgmt 层通用错误被兜成 500，参数错误不应按服务端故障处理，handler 层提前校验回 400。
13. **列表行字段不稳定（E2E 验收发现）**：部分驱动 GetMany 对 NULL 列整键缺失，同一接口的行有时有 `deleted_at`/`expires_at` 有时没有。`normalizeListRow` 预填默认空键，响应字段集合恒定。

## 二、验收发现的其他问题（均已修）
14. **集成测试污染共享库（重要）**：`schema_sync_integration_test.go` 的 `t.Cleanup(cleanup)` 晚于 `defer d.Close()` 执行，DROP 落在已关闭连接上被静默吞掉 → 每次跑完集成测试都在 devdb 留下一个**缺唯一约束的旧版 ip_blacklist 表**。这个残留表曾让 E2E 的「重复新增」成功写入重复行、pg 的 ON CONFLICT 导入直接报错。改 `defer cleanup()`（连接关闭前执行）。连带清理了两个库里的遗留表。
15. **小黑屋空态 `rows:null`**：改回 `[]`（Jail 入口预初始化）。
16. **共享 devdb 跨包互踩**：`internal/db` 全清单验收（DROP/CREATE 7 张规范表）与 `plugins/shield` 的 `TestPostgresAttackArchive`（固定表名 attack_archive）在 go test 多包并行时互踩。加文件锁 `os.TempDir()/rocksys-devdb-it.lock`（flock）串行化。注意锁必须固定路径（`os.CreateTemp` 每次随机文件名等于没锁，已踩过）；且同一 Test 内不可重复加锁（同进程二次 LOCK_EX 自阻塞，曾把测试挂满 10 分钟超时）。

## 三、已知边界 / 留存事项（未修，属设计范围外）
- **存量表「列在但约束被删」不在 schema diff 检测范围**：C 级只覆盖「缺约束列」；表级 UNIQUE 被人为删除这类情况要靠人工（可走「执行SQL」手写补）。已在 DB_SCHEMA_SYNC_PLAN.md 记为已知边界。这次 mysql/pg 两个 devdb 都出现过这种遗留表（正是它让 E2E 暴露了一串问题），已清除。
- **easyconf 装配期 Register 无锁**：并发安全的读路径只保证在装配完成后使用（引擎 Start 已后移）；若未来支持运行期注册需改 easyconf。
- **小黑屋 rows 与 total 两次查询非一致快照**：影响极小，维持现状（原审核低优先项）。
- **Jail 快照一致性、`SyncBlacklistFile` 等其余低优先审核项**：见 git 提交信息，均已按「修或不修」明确处理，未修的都是记录在案的可接受项。

## 四、验收执行记录
- **自动化**：`go test ./...`、`go test -race ./...`（0 race）、`go vet ./...`、`go vet -tags integration ./...`、`go test -tags integration ./...`（真库 mysql+pg，DSN 门控：`MYSQL_TEST_DSN="dev:dev123456@tcp(127.0.0.1:3306)/devdb?parseTime=true"`、`PG_TEST_DSN="postgresql://dev:dev123456@127.0.0.1:5432/devdb?sslmode=disable"`）、生产构建 `go build ./cmd/rocksys` —— 全部通过。
- **新增验收测试**：`internal/db/schema_sync_acceptance_integration_test.go`（全新库 7 表 A 级一轮闭环 / 单索引缺失 D 级精确补齐 / catalog 自增·默认值·可空识别）、`plugins/shield/devdb_lock_integration_test.go`（共享库锁）。
- **端到端功能验收**（真服务 `bin/rocksys` + 真 mysql/pg 各 27 项断言，脚本 `/tmp/e2e_acceptance.sh`）：表结构检查方言正确、缺表 A 级、A 级 SQL 附带建索引、执行 failed=0、两轮复核零差异、单索引缺失恰 1 个 D 级且不重放未缺失索引；block_type 缺省 11/越界 400/重复新增恢复指引、导入幂等（2 新 1 跳过）、sort 白名单生效与非法回退、ban 三态（新封/活跃 400 已在黑名单/软删恢复永久且 warn=2）、小黑屋口径（永久不在押、限时在押、total）、duration 非法指引、sync_file、events 端点 —— mysql/pg 均 27/27。
- **plan 文档删除**：`docs/plan/`（TODO.md + db_schema_sync STEP1-5 + ip_blacklist STEP1-8）已随提交删除；`docs/IP_BLACKLIST_PLAN.md`、`docs/DB_SCHEMA_SYNC_PLAN.md` 保留并补验收记录。

## 五、给后续操作者的提醒
- `bin/hotscripts/sql/` 是发布外挂脚本（外挂优先、内嵌兜底），改 `sql/` 后必须同步刷新（`cp -r sql/* bin/hotscripts/sql/`），否则服务端用的还是旧脚本——本次 E2E 一度因此误判（旧版 restore_ban.sql 5 参数 vs 新代码 4 参数 → 永久封禁 500）。
- 共享 devdb 出现「重复 IP 能插入」时先查表有没有唯一约束（历史遗留表不会被 `CREATE TABLE IF NOT EXISTS` 修复），可用表结构同步页或直接 DROP 让服务重建。
- easyconf 日志模板只渲染 msg 不输出 attr，排查错误细节时可临时用探针程序直查。
