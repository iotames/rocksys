# WAF 风险路径规则：每行一个精确路径或目录前缀（小写匹配）。
# 目录前缀（如 /.well-known）自动匹配其下全部子路径。
# 以 # 开头的行是注释，空行忽略。外挂覆写目录 HOT_SCRIPTS_DIR/rules/ 同名文件可覆盖本文件，无需重新编译。
/.env
/.git
/.svn
/.hg
/.htaccess
/.DS_Store
/.well-known
/phpmyadmin
/pma
/wp-admin
/wp-login.php
/config.php
/web.config
/server-status
/server-info
/crossdomain.xml
/actuator
/console